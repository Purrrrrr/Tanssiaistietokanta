# Tools

This folder contains tools used in this projects.

Currently it only holds the seamless tool to make seamless header bar images for the frontend.
Run it with `python seamless.py` to see its usage or run `python seamless.py filename` to generate a seamless image from the given file. The output will be saved as `filename_seamless.webp` in the same directory as the input file.
