const feathers = require('@feathersjs/feathers');
const processEventProgramInput = require('../../src/hooks/process-event-program-input');

describe('\'processEventProgramInput\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      before: processEventProgramInput()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
