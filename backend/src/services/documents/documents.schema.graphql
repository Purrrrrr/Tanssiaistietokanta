scalar DocumentContent

type Query {
  document(id: ID!, versionId: ID): Document!
  documents(owner: DocumentOwner!, owningId: ID!, path: String): [Document!]!
}

type Document implements Versionable {
  _id: ID!
  _versionId: String
  _versionNumber: String
  _updatedAt: String!
  _createdAt: String!
  title: String!
  content: DocumentContent
  owner: DocumentOwner!
  owningId: ID!
  path: String!
  versionHistory: VersionHistory!
}

enum DocumentOwner {
  dances
  events
  workshops
}

type Mutation {
  createDocument(owner: DocumentOwner!, owningId: ID!, path: String, title: String!, content: DocumentContent): Document!
  patchDocument(id: ID!, document: JSONPatch!): Document!
  deleteDocument(id: ID!): Document!
}
