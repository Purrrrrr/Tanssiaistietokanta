type Query {
  file(id: ID!): File!
  files(root: String!, path: String): [File!]!
}

type File {
  _id: ID!
  _createdAt: String!
  _updatedAt: String!
  root: String!
  path: String!
  name: String!
  mimetype: String!
  size: Int!
}

type Mutation {
  renameFile(id: ID!, name: String!, path: String): File!
  moveFile(id: ID!, name: String!, path: String): File!
  deleteFile(id: ID!): File!
}
