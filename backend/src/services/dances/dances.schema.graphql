type Query {
  dances: [Dance!]!
}

type Dance {
  _id: ID
  name: String!
  description: String
  duration: Float
  prelude: String
  formation: String
  remarks: String
  deleted: Boolean!
}

type Mutation {
  createDance(dance: DanceInput!): Dance!
  modifyDance(id: ID!, dance: DanceInput!): Dance!
  deleteDance(id: ID!): Dance!
}

input DanceInput {
  name: String!
  description: String
  duration: Float
  prelude: String
  formation: String
  remarks: String
}
